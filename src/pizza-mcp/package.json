{
  "name": "pizza-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": "./lib/local.js",
  "scripts": {
    "start": "tsx src/server.ts",
    "start:local": "tsx src/local.ts",
    "dev": "tsx watch src/server.ts",
    "dev:local": "tsx watch src/local.ts",
    "build": "tsc",
    "clean": "rimraf lib",
    "docker:build": "docker build -t pizza-mcp .",
    "docker:run": "docker run -p 3000:3000 -e PORT=3000 pizza-mcp"
  },
  "keywords": [],
  "author": "Microsoft",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "dotenv": "^17.2.1",
    "express": "^5.0.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.17.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.3",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  }
}
